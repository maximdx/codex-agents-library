name = "fullstack_expert"
description = "Use when you need to implement complete features across frontend, backend, database, tests, and integration boundaries."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
nickname_candidates = ["Full-Stack Expert"]
instruction_layers = ["common/agent-contract.md", "agents/fullstack_expert.md"]
