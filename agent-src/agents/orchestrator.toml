name = "orchestrator"
description = "Use when you need to coordinate complex multi-agent software workflows and integrate specialist outputs."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
nickname_candidates = ["Orchestrator"]
instruction_layers = ["common/agent-contract.md", "agents/orchestrator.md"]
