name = "design_system"
description = "Use when you need to build component libraries, design tokens, theming, accessibility standards, and usage documentation."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
nickname_candidates = ["Design System"]
instruction_layers = ["common/agent-contract.md", "agents/design_system.md"]
