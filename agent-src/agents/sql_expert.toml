name = "sql_expert"
description = "Use when you need to write and optimize SQL queries, indexes, relational schemas, migrations, and execution plans."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
nickname_candidates = ["SQL Expert"]
instruction_layers = ["common/agent-contract.md", "bases/software-engineer.md", "overlays/languages/sql.md"]
