name = "workflow_manager"
description = "Use when you need to design sequential and parallel execution workflows with dependencies, retries, and handoffs."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
nickname_candidates = ["Workflow Manager"]
instruction_layers = ["common/agent-contract.md", "agents/workflow_manager.md"]
