name = "research_agent"
description = "Use when you need to research technical options, documentation, tradeoffs, and current best practices with source citations."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
nickname_candidates = ["Research Agent"]
instruction_layers = ["common/agent-contract.md", "agents/research_agent.md"]
