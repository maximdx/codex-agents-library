name = "performance_optimizer"
description = "Use when you need to find bottlenecks and improve runtime, queries, memory, bundle size, and latency."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
nickname_candidates = ["Performance Optimizer"]
instruction_layers = ["common/agent-contract.md", "agents/performance_optimizer.md"]
