name = "legacy_modernizer"
description = "Use when you need to plan and execute incremental modernization, refactors, migrations, and technical debt reduction."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
nickname_candidates = ["Legacy Modernizer"]
instruction_layers = ["common/agent-contract.md", "agents/legacy_modernizer.md"]
