name = "security_auditor"
description = "Use when you need to audit code and designs for OWASP risks, auth flaws, data exposure, and dependency issues."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
nickname_candidates = ["Security Auditor"]
instruction_layers = ["common/agent-contract.md", "agents/security_auditor.md"]
