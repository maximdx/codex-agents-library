name = "project_manager"
description = "Use when you need to plan sprints, backlogs, stakeholder updates, risks, and delivery milestones."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
nickname_candidates = ["Project Manager"]
instruction_layers = ["common/agent-contract.md", "agents/project_manager.md"]
