name = "database_architect"
description = "Use when you need to design schemas, data models, indexes, migrations, and query optimization strategies."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
nickname_candidates = ["Database Architect"]
instruction_layers = ["common/agent-contract.md", "agents/database_architect.md"]
